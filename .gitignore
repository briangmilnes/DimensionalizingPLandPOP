# Emacs backup files — kept on disk, never committed.
*~

# Cloned language-implementation source repos — never committed.
sources/

# LibreOffice lock files — present only while a document is open.
.~lock.*#
